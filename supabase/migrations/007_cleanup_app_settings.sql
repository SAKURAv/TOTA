-- ============================================================
-- مراجعة app_settings بعد إلغاء الشات (006_remove_chat.sql):
-- الجدول كان بيتستخدم بس عشان "مدة الاحتفاظ بالشات" (chat_retention_days)،
-- ودي بقت من غير معنى خالص بعد إلغاء نظام الشات بالكامل.
--
-- كل إعدادات التواصل الفعلية (رقم واتساب، رسالة واتساب الجاهزة، اسم
-- المتجر، السوشيال ميديا) مُدارة أصلاً من data/config.json عن طريق
-- البرنامج (شاشة "بيانات التواصل" tota_admin2) ومنشورة عبر GitHub —
-- مفيش داعي نكررها في جدول DB منفصل.
--
-- القرار: نحذف الجدول بالكامل بدل ما نسيبه فاضي بلا استخدام. لو احتجنا
-- مستقبلًا إعداد لازم يتغيّر فورًا من غير نشر GitHub (زي فلاج تشغيل/إيقاف
-- الموقع مؤقتًا)، هننشئه وقتها بجدول واضح الغرض.
-- ============================================================

drop table if exists public.app_settings;
